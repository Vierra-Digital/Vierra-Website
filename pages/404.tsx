import NotFoundContent from "@/components/NotFoundContent"

/**
 * Pages Router 404. Body shared with app/not-found.tsx — see the note there.
 */
export default function Custom404() {
  return <NotFoundContent />
}
