/node_modules
/.pnp
.env
/.env.local
/.env.prod
/.env.production
/.env.development
/.env.backup*
/.env.*.bak
db_dumps/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
/coverage
.next/
.netlify/
/.vercel
/out/
/build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel
*.tsbuildinfo
next-env.d.ts
/lib/generated/prisma
.env
todo.txt
/scripts/create-client.js
/scripts/generate-env.js
/scripts/sync-db.js
/scripts/update-env.js
/scripts/update-env.js
/scripts/README.md
.vscode/
# Local secrets / service-account keys — never commit
secrets/

# Local .env backups — these hold live credentials.
.env.bak*
.env.*.bak

# Prisma 7 generates the client here instead of node_modules.
lib/generated/

# Next writes AGENTS.md on every `next dev` (see
# node_modules/next/dist/server/lib/generate-agent-files.js). It is not wanted in this repo, so it
# is ignored rather than committed — otherwise every dev run leaves an uncommitted change.
AGENTS.md
